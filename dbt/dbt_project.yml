name: 'sec_restatement'
version: '1.0.0'
config-version: 2
profile: 'sec_restatement'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  sec_restatement:
    staging:
      +materialized: view
      +schema: staging
    analysis:
      +materialized: table
      +schema: analysis
      +tags: ['dq']
    marts:
      +materialized: table
      +schema: marts
